# Local README preview tooling (not part of the published protocol)
.preview/
.claude/

# PayneSDD per-task gate marker + its block counter (runtime, never committed)
.payne-active*

# OS / editor noise
.DS_Store
*.swp

# local benchmark harness + run artifacts (never committed; see benchmark/local/README.md)
benchmark/local/
